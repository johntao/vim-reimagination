
{ //filling the window with *
  // for (int i = 0; i < winHEI; i++)
  // {
  //   for (int j = 0; j < winWID; j++)
  //   {
  //     Console.Write("*");
  //   }
  //   Console.WriteLine();
  // }
}
{ //testing window
  // winWID = Console.WindowWidth; //209
  // winHEI = Console.WindowHeight; //51
  // Console.WriteLine($"{winWID}x{winHEI}");
  // // Console.Write(new string('*', 209));
  // // Console.WriteLine(new string('.', 200));
  // // Console.
  // // Console.SetWindowSize(209, 51);
  // winWID = Console.WindowWidth; //209
  // winHEI = Console.WindowHeight; //51
  // Console.WriteLine($"{winWID}x{winHEI}");
  // Console.Write(new string('*', 209));
  // Console.WriteLine(new string('.', 200));
}
{ //testing buffer
  // var bufWID = Console.BufferWidth;
  // var bufHEI = Console.BufferHeight;
  // Console.WriteLine($"{winWID}x{winHEI}");
  // Console.WriteLine($"{bufWID}x{bufHEI}");
  // #pragma warning disable CA1416 // Validate platform compatibility
  // Console.SetBufferSize(209, 51);
  // #pragma warning restore CA1416 // Validate platform compatibility
  // winWID = Console.WindowWidth; //209
  // winHEI = Console.WindowHeight; //51
  // bufWID = Console.BufferWidth;
  // bufHEI = Console.BufferHeight;
  // Console.WriteLine($"{winWID}x{winHEI}");
  // Console.WriteLine($"{bufWID}x{bufHEI}");
  // Console.Write(new string('*', 209));
  // Console.WriteLine(new string('.', 200));
}
// #pragma warning disable CA1416 // Validate platform compatibility
// Console.WriteLine(Console.CursorVisible);
// #pragma warning restore CA1416 // Validate platform compatibility

// var ww = Encoding.ASCII.GetBytes("2nd Hello World!\n");
// qq.Write(ww, 0, ww.Length);
// Console.ReadKey(true);
{ //test StringBuilder API
  // int i = -1;
  // foreach (ReadOnlyMemory<char> chunk in Buffer.GetChunks())
  // {
  //   SpanLineEnumerator ff = chunk.Span.EnumerateLines();
  //   foreach (ReadOnlySpan<char> item in ff)
  //   {
  //     Console.WriteLine($"{++i} {new string(item)}");
  //   }
  //   // var span = chunk.Span;
  //   // for(int i = 0; i < span.Length; i++)
  //   // {
  //   //     /* operations on span[i] */
  //   // }
  // }
}

{ //testing ReadOnlySpan API
  ReadOnlySpan<char> qq = """
  aabbbcccc ddeeeffff gghhhiiii
  """;
  // qq.IndexOfAny()
  Console.WriteLine($"qq.IndexOfAnyInRange('d', 'e') --> {qq.IndexOfAnyInRange('d', 'e')}"); //10
  Console.WriteLine($"qq.IndexOfAnyInRange('d', 'f') --> {qq.IndexOfAnyInRange('d', 'f')}"); //10
  Console.WriteLine($"qq.IndexOfAnyInRange('e', 'f') --> {qq.IndexOfAnyInRange('e', 'f')}"); //12
  Console.WriteLine($"qq.IndexOfAnyInRange('f', 'g') --> {qq.IndexOfAnyInRange('f', 'g')}"); //15
  Console.WriteLine($"qq.IndexOfAnyInRange('f', 'h') --> {qq.IndexOfAnyInRange('f', 'h')}"); //15
  Console.WriteLine($"qq.IndexOfAnyInRange('f', 'i') --> {qq.IndexOfAnyInRange('f', 'i')}"); //15
}
{ //testing ReadOnlySpan API
  ReadOnlySpan<char> qq = """
  aabbbcccc ddeeeffff gghhhiiii
  """;
  // qq.IndexOfAny()
  Console.WriteLine($"qq.IndexOfAnyInRange('d', 'e') --> {qq.IndexOfAny(SearchValues.Create("de"))}"); //10
  Console.WriteLine($"qq.IndexOfAnyInRange('d', 'f') --> {qq.IndexOfAny(SearchValues.Create("df"))}"); //10
  Console.WriteLine($"qq.IndexOfAnyInRange('e', 'f') --> {qq.IndexOfAny(SearchValues.Create("ef"))}"); //12
  Console.WriteLine($"qq.IndexOfAnyInRange('f', 'g') --> {qq.IndexOfAny(SearchValues.Create("fg"))}"); //15
  Console.WriteLine($"qq.IndexOfAnyInRange('f', 'h') --> {qq.IndexOfAny(SearchValues.Create("fh"))}"); //15
  Console.WriteLine($"qq.IndexOfAnyInRange('f', 'i') --> {qq.IndexOfAny(SearchValues.Create("fi"))}"); //15
}